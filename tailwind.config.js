/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ['./src/**/*.{js,jsx,ts,tsx}'], // Combining the content paths from both files
  theme: {
    extend: {}, // Extend remains the same
  },
  plugins: [], // Plugins remain the same
};
